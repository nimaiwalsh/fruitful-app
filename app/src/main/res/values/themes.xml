<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- The actual theme we use. This varies for light theme (here),
        and values-night for dark theme. -->
    <style name="Theme.Fruitful" parent="Platform.Theme.Fruitful">
        <!-- Primary brand color. -->
        <!-- HANDLED BY COMPOSE THEME -->
        <!-- Customize your theme here. -->
    </style>

    <!-- Allows us to override platform level specific attributes in their
       respective values-vXX folder. -->
    <style name="Platform.Theme.Fruitful" parent="Theme.Material3.DayNight.NoActionBar">
    </style>

    <style name="Theme.Fruitful.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Fruitful.AppBarOverlay" parent="ThemeOverlay.Material3.Dark.ActionBar" />

    <style name="Theme.Fruitful.PopupOverlay" parent="ThemeOverlay.Material3.Light" />
</resources>